const GeneralTabContent = () => {
  return (
    <div>
      <h3>Game Overview</h3>
      {/* TODO: Fetch and render API data */}
      <p>Umpires:</p>
      <p>Weather:</p>
      <p>Wind:</p>
      <p>Venue:</p>
      <p>First pitch:</p>
      <p>Last pitch:</p>
      <p>Time:</p>
    </div>
  );
};

export default GeneralTabContent;
